**Deferred:** pointing a real domain at the production Vercel deployment.
Pre-launch, `main` deploys to a Vercel-assigned `*.vercel.app` URL only.

**From:** kickoff (`01-architecture.md` deploy model).

**Closes when:** at the attended launch event, a domain is chosen and
pointed at the production deployment (or this deferral is explicitly
closed as "not needed — `*.vercel.app` is fine for launch" if the user
decides not to buy a domain).
