**Deferred:** live (production) Supabase project + keys. Dev/CI/preview use
the dev-dedicated Supabase project's test-mode keys only.

**From:** kickoff (`01-architecture.md` environment contract).

**Closes when:** at the attended launch event, a production Supabase
project is created and its keys replace the dev project's in Vercel's
production environment variables — a deliberate, human-triggered swap, never
automated.
